application:
  name: textgen
  pods:
  - name: app
    image: mirror.gcr.io/atinoda/text-generation-webui:default-cpu
    path: /
    servicePorts:
    - 7860
    vars:
      EXTRA_LAUNCH_ARGS: "--listen --listen-port 7860 --api-port 5000"
